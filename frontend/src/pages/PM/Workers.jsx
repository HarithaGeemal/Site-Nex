import React, { useState } from 'react';
import { workers } from '../../../assets/dummyData';

const Workers = () => {
    return (
        <div className="p-6">
            <h1 className="text-2xl font-bold mb-4">Worker Management</h1>
            <p>Manage workers, details, and task assignments here.</p>
        </div>
    );
};

export default Workers;
